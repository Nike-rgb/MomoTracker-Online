<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title> <%= title %> </title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.0.2/tailwind.min.css" integrity="sha512-+WF6UMXHki/uCy0vATJzyA9EmAcohIQuwpNz0qEO+5UeE5ibPejMRdFuARSrl1trs3skqie0rY/gNiolfaef5w==" crossorigin="anonymous" />
    <link href="https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,300;0,900;1,400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css" />
    <!--font-family: 'Lato', sans-serif;-->

  </head>
  <body>
    <nav>
      <div class="logo-wrapper">
        <a href = "/" ><img src='/img/momo.svg' alt="logo"></a>
        <span>Momos'</span>
      </div>
      <% if(user) {%> <div class="logged-in-name"> Hi <%= user.name %> </div> <% } %>
       <div class="nav-wrapper">
         <ul>
           <li><a href="/">Menu</a></li>|
           <% if(!user) { %>
           <li><a href="/register">Register</a></li>|
           <li><a href="/login">Login</a></li>|
           <% } else { %>
             <% if(user.role == "admin") { %>
               <li><a href="/admin/orders">Orders</a></li>|
               <% } %>
            <li><a href="/logout">Logout</a></li>|
             <% } %>
           <% if(!cart) { %> <li><span class="total-counter">0</span><a href="/cart"><i style="font-size:28px" class="fas fa-shopping-cart"></i></a></li>|
           <% } else { %>
             <li><span class="total-counter"> <%= cart.totalQty %> </span><a href="/cart"><i style="font-size:28px" class="fas fa-shopping-cart"></i></a></li>|
            <% } %>
         </ul>
       </div>
     </nav>

 <%- body %>


  </body>
</html>
